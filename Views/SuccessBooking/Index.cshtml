<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-body text-center">
            <svg xmlns="http://www.w3.org/2000/svg" width="75" height="75" fill="green" class="mb-3" viewBox="0 0 16 16">
                <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zM7 11.414l5.707-5.707-1.414-1.414L7 8.586 4.707 6.293 3.293 7.707 7 11.414z" />
            </svg>
            <h2 class="text-success">Booking Successful!</h2>
            <p class="mt-3">Thank you for your booking. A confirmation has been sent to your email.</p>
            <a href="/" class="btn btn-primary mt-4">Back to Home</a>
        </div>
    </div>
</div>
<script>
    function getCookie(name) {
        const value = `; ${document.cookie}`;
        const parts = value.split(`; ${name}=`);
        if (parts.length === 2) return parts.pop().split(';').shift();
        return '';
    }

    function deleteCookie(name) {
        document.cookie = name + '=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;';
    }

    document.addEventListener("DOMContentLoaded", function () {
        const COOKIE_ROOM_ID = 'ROOM_ID';
        const COOKIE_PRICE = 'PRICE';

        if (getCookie(COOKIE_ROOM_ID)) {
            deleteCookie(COOKIE_ROOM_ID);
        }
        if (getCookie(COOKIE_PRICE)) {
            deleteCookie(COOKIE_PRICE);
        }
    });
</script>
